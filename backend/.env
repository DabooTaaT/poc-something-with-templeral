# API Configuration
API_PORT=8080

# Database Configuration
DATABASE_URL=postgres://workflow_user:workflow_pass@localhost:5432/workflow_db?sslmode=disable

# Temporal Configuration
TEMPORAL_HOST=localhost:7233

# CORS Configuration
# WARNING: Using "*" allows ALL origins - NOT recommended for production!
# For development only. In production, specify exact domains.
CORS_ALLOWED_ORIGINS=*
